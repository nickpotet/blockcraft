{
  "name": "blockcraft",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "textures": "node scripts/generate-textures.mjs && node scripts/generate-item-icons.mjs"
  },
  "dependencies": {
    "three": "^0.177.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
